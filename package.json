{
  "name": "nvm-node-switch",
  "displayName": "NVM Node Switch",
  "description": "基于.nvmrc的Node版本自动切换工具，提供以下功能：\n- 自动检测并切换工作区.nvmrc指定版本\n- 智能推荐package.json适配的Node版本\n- 可视化管理已安装/可用Node版本（安装/切换/删除）\n- 支持nvm、nodejs官方和npmmirror多版本源\n- 提供跨平台的版本管理Webview界面",
  "version": "0.1.16",
  "publisher": "李学明",
  "repository": {
    "type": "git",
    "url": "https://github.com/FoodGravity/NVMNodeSwitch.git"
  },
  "engines": {
    "vscode": "^1.97.2"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "nodeVersionManager",
          "title": "Node Version Manager",
          "icon": "assets/icons/icon.svg"
        }
      ]
    },
    "views": {
      "nodeVersionManager": [
        {
          "id": "nodeVersionManagerWebview",
          "name": "Node 版本管理器",
          "type": "webview"
        }
      ]
    },
    "commands": [],
    "menus": {
      "view/item/context": [],
      "view/title": [],
      "editor/context": []
    }
  },
  "scripts": {
    "Update package": "node update-version.js && node ./esbuild.js --production && vsce package",
    "vscode:prepublish": "npm run package",
    "compile": "node ./esbuild.js",
    "watch": "node ./esbuild.js --watch",
    "package": "node ./esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/iconv-lite": "^0.0.1",
    "@types/mocha": "^10.0.7",
    "@types/node": "20.x",
    "@types/vscode": "^1.97.2",
    "@typescript-eslint/eslint-plugin": "^8.3.0",
    "@typescript-eslint/parser": "^8.3.0",
    "esbuild": "^0.23.0",
    "eslint": "^9.9.1",
    "typescript": "^5.5.3",
    "vscode-test": "^1.6.1"
  },
  "dependencies": {
    "iconv-lite": "^0.6.3"
  }
}